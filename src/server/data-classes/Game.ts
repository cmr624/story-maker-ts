/* 
Game - the parent container for rounds and players (this would be where firebase / lobby code exists
    Game has at least one round
    Progress round
    Add / remove player
    Serializable class (maybe) */

export default class Game{};